#!/bin/sh

. /etc/default/noname

[ -z "$QT_PLATFORM" ] && exit 1

sv start /service/dbus /service/systemstate || exit 1

exec /bin/su - -c "/usr/bin/dbus-launch --sh-syntax --exit-with-session /usr/bin/noname -platform $QT_PLATFORM" user
